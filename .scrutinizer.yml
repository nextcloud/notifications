filter:
    excluded_paths:
        - '3rdparty/*'
        - 'js/jquery*'
        - 'l10n/*'
        - 'tests/*'

imports:
    - javascript
    - php

tools:
    external_code_coverage:
        timeout: 7200 # Timeout in seconds: 2 hours
